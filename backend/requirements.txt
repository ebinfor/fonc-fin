# FONCIER+ v3.5.2 — Dépendances (FIX-04 : email-validator ajouté)

# FastAPI stack
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# SQLAlchemy async
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
psycopg2-binary==2.9.9

# Pydantic
pydantic==2.8.2
pydantic-settings==2.4.0
email-validator==2.1.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# Cache / stockage
redis==5.1.0
minio==7.2.9

# Géospatial (optionnel — nécessite GDAL système si activé)
# geoalchemy2==0.15.2   # décommenter si PostGIS activé côté système

# Documents
reportlab==4.2.2
jinja2==3.1.4

# Monitoring
sentry-sdk[fastapi]==2.14.0

# Tests (dev uniquement)
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
